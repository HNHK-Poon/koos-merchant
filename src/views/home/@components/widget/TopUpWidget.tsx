import React, { useState } from 'react'
import PropTypes from 'prop-types'

const TopUpWidget = (props:any) => {
    const [amount, setAmount] = useState('')
  return (
    <div>TopUpWidget</div>
  )
}

TopUpWidget.propTypes = {}

export default TopUpWidget